---
- name: playbook example
  gather_facts: false
  become: false
  hosts: all
  tasks:
    - name: Install the latest version of Apache and MariaDB
      package:
        name: "{{ item }}"
        state: present
      with_items:
        - httpd
        - mariadb-server
